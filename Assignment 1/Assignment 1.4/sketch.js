var yPos = 0;
var xPos = 0;
function setup() {
    createCanvas(500,250);
     // setup() runs once
      frameRate(30);
      noLoop();
    }
    function draw() {
     // draw() loops forever, until stopped
      background('aqua');
      stroke(0);
      yPos = yPos - 1;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 1;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 1;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 1;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 2;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 2;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 3;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 3;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 4;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 4;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 5;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 5;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 6;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 6;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 7;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 7;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 8;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 8;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 9;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 9;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 10;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 10;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 11;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 11;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 12;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 12;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 13;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 13;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 14;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 14;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 15;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 15;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);



      stroke('Magenta');
      
      yPos = yPos - 4;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 4;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 5;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 5;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 6;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 6;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 7;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 7;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 8;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 8;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 9;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 9;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 10;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 10;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 11;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 11;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 12;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 12;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 13;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 13;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 14;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 14;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
      yPos = yPos - 15;
      if (yPos < 0) {
        yPos = height;
      }
      xPos = xPos + 15;
      if (xPos < 0) {
        xPos = width;
      }
      line(0, yPos, width, yPos);
      line(0, xPos, width, xPos);
      line(0, xPos, width, yPos);
      line(0, yPos, width, xPos);
    }
